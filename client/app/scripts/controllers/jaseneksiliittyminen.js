'use strict';

/**
 * @ngdoc function
 * @name karvinenApp.controller:JaseneksiliittyminenCtrl
 * @description
 * # JaseneksiliittyminenCtrl
 * Controller of the karvinenApp
 */
angular.module('karvinenApp')
  .controller('JaseneksiliittyminenCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
