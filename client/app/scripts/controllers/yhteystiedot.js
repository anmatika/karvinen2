angular.module('karvinenApp')
	.controller('YhteystiedotCtrl', function($scope, $rootScope) {
		// $rootScope.title = "Karvinen Ry."

		// $scope.$on('mapInitialized', function(evt, map) {
		//  	console.log('mapinitilalized')
		// 	// var infoWindow = map.infoWindows[0];
			
			
		// });
	});